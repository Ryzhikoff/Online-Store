<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/tag_view_background"
    android:orientation="horizontal"
    android:paddingStart="@dimen/padding_12"
    android:paddingEnd="@dimen/padding_12"
    android:paddingTop="@dimen/padding_5"
    android:paddingBottom="@dimen/padding_5">

    <TextView
        android:id="@+id/name"
        style="@style/Title4"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/gray"
        tools:text="Смотреть все" />

    <ImageView
        android:id="@+id/icon"
        android:layout_width="@dimen/icon_size_tag_view"
        android:layout_height="@dimen/icon_size_tag_view"
        android:layout_marginStart="@dimen/margin_4"
        android:layout_marginEnd="-4dp"
        android:src="@drawable/icon_small_close"
        tools:tint="@color/red"
        tools:ignore="ContentDescription" />

</LinearLayout>